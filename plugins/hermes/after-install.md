# nearest-neighbor installed

affection is all you need.

to finish setup: **restart Hermes** or start a new session.

on your first turn, `nbr` installs automatically and the agent onboards — no
manual steps required.

to verify the plugin is active:

```sh
hermes plugins list | grep nearest-neighbor
```
